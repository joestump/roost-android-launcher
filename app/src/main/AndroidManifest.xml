<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Best-effort "force stop" for the Agent → Restart action (kills the agent's background procs). -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <!-- Enumerate other launchable apps for the favorites picker (Android 11+ package visibility). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:clearTaskOnLaunch="true"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title" />

        <!-- ADR-0005: settings landing → per-category detail Activities. -->
        <activity
            android:name=".BehaviorActivity"
            android:exported="false"
            android:label="@string/settings_title" />

        <activity
            android:name=".AgentActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/settings_title" />

        <activity
            android:name=".AppearanceActivity"
            android:exported="false"
            android:label="@string/settings_title" />

        <activity
            android:name=".AppsActivity"
            android:exported="false"
            android:label="@string/settings_title" />

        <activity
            android:name=".AppPickerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/settings_title" />

        <activity
            android:name=".WebAppsActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/settings_title" />

        <activity
            android:name=".HiddenActivity"
            android:exported="false"
            android:label="@string/settings_title" />

        <activity
            android:name=".NetworkActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/settings_title" />

        <activity
            android:name=".WebAppActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".ActionsActivity"
            android:exported="false"
            android:label="@string/actions_title" />

        <activity
            android:name=".IconPickerActivity"
            android:exported="false"
            android:label="@string/icon_picker_title" />

        <activity
            android:name=".HttpActionActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".EndpointsActivity"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
